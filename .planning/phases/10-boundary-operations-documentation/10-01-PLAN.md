---
phase: 10-boundary-operations-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/quantization/01-boundary-operations.md]
autonomous: true

must_haves:
  truths:
    - "QuantizeLinear formula is documented with exact ONNX spec notation"
    - "DequantizeLinear formula is documented with exact ONNX spec notation"
    - "Both symmetric and asymmetric quantization cases are explained"
    - "Round-trip error bounds are documented with mathematical proof"
    - "Numerical examples demonstrate each operation with concrete values"
    - "Documentation renders correctly on GitHub with MathJax"
  artifacts:
    - path: "docs/quantization/01-boundary-operations.md"
      provides: "Complete boundary operations documentation"
      contains: "QuantizeLinear"
  key_links:
    - from: "docs/quantization/01-boundary-operations.md"
      to: "ONNX QuantizeLinear spec"
      via: "exact formula notation"
      pattern: "saturate.*round.*y_scale"
    - from: "docs/quantization/01-boundary-operations.md"
      to: "ONNX DequantizeLinear spec"
      via: "exact formula notation"
      pattern: "x_scale.*x_zero_point"
---

<objective>
Create comprehensive documentation for ONNX QuantizeLinear and DequantizeLinear boundary operations, covering exact formulas, parameter definitions, symmetric/asymmetric cases, numerical examples, and round-trip error analysis.

Purpose: Provide reference documentation for hardware engineers implementing FP32-to-INT8 and INT8-to-FP32 conversions at model input/output boundaries.

Output: `docs/quantization/01-boundary-operations.md` - A complete specification document that renders correctly on GitHub with MathJax math formulas.
</objective>

<execution_context>
@/home/impactaky/shelffiles/config/claude/get-shit-done/workflows/execute-plan.md
@/home/impactaky/shelffiles/config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-boundary-operations-documentation/10-CONTEXT.md
@.planning/phases/10-boundary-operations-documentation/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create boundary operations documentation</name>
  <files>docs/quantization/01-boundary-operations.md</files>
  <action>
Create `docs/quantization/01-boundary-operations.md` with the following structure:

**Document Structure:**

1. **Title and Overview**
   - Brief introduction to boundary operations (FP32 <-> INT8 conversions)
   - Reference to ONNX specification as authoritative source

2. **QuantizeLinear Operation** (main section)
   - Formula: `y = saturate(round(x / y_scale) + y_zero_point)`
   - Use display math ($$...$$) for main formula, escape underscores as `y\_scale`
   - Parameter definitions using ONNX names exactly: `x`, `y_scale`, `y_zero_point`
   - Explain rounding: round-to-nearest-even (banker's rounding) with tie examples (0.5 -> 0, 1.5 -> 2)
   - Saturation ranges table (int8: [-128, 127], uint8: [0, 255], include int16/uint16 for completeness)
   - **Symmetric case (y_zero_point = 0):** Simplified formula, characteristics (zero maps to 0)
   - **Asymmetric case:** Full formula, better range utilization, when to use
   - **Numerical example:** Simple values (e.g., x=2.7, y_scale=0.1) showing all calculation steps

3. **DequantizeLinear Operation** (main section)
   - Formula: `y = (x - x_zero_point) * x_scale`
   - Note parameter naming: uses `x_` prefix because operates on quantized input
   - Parameter definitions using ONNX names exactly
   - **Numerical example:** Reverse of QuantizeLinear example, show dequantization

4. **Round-Trip Relationship**
   - Prove `Dequant(Quant(x)) ≈ x`
   - Error bound formula: `|x - Dequant(Quant(x))| <= y_scale/2` for values within range
   - Document saturation behavior for out-of-range values
   - Numerical example showing round-trip with error calculation

5. **Data Type Reference**
   - Table of all supported quantization types with their ranges (uint8, int8, uint16, int16, uint4, int4, uint2, int2)
   - Note: y_zero_point type determines output type

**Format Guidelines (from CONTEXT.md):**
- Markdown format, not LaTeX-heavy
- Show formulas directly without step-by-step derivation
- Describe how each formula is formed
- Use ONNX spec variable names exactly
- NO hardware pseudocode (explicitly deferred per user decision)

**Math Syntax (from RESEARCH.md):**
- Display math: `$$y = \text{saturate}\left(\text{round}\left(\frac{x}{y\_scale}\right) + y\_zero\_point\right)$$`
- Escape underscores in LaTeX: `y\_scale` not `y_scale`
- Inline math: `$y\_scale$` for variable references

**Content Sources:**
- ONNX QuantizeLinear spec: https://onnx.ai/onnx/operators/onnx__QuantizeLinear.html
- ONNX DequantizeLinear spec: https://onnx.ai/onnx/operators/onnx__DequantizeLinear.html
  </action>
  <verify>
1. File exists: `ls docs/quantization/01-boundary-operations.md`
2. Contains QuantizeLinear: `grep -q "QuantizeLinear" docs/quantization/01-boundary-operations.md`
3. Contains DequantizeLinear: `grep -q "DequantizeLinear" docs/quantization/01-boundary-operations.md`
4. Contains round-trip section: `grep -q "Round-Trip" docs/quantization/01-boundary-operations.md`
5. Contains both cases: `grep -q "Symmetric" docs/quantization/01-boundary-operations.md && grep -q "Asymmetric" docs/quantization/01-boundary-operations.md`
6. Uses ONNX naming: `grep -q "y_scale" docs/quantization/01-boundary-operations.md`
7. Has numerical examples: `grep -q "Example" docs/quantization/01-boundary-operations.md`
  </verify>
  <done>
- QuantizeLinear documentation includes exact formula, symmetric/asymmetric cases, and numerical example
- DequantizeLinear documentation includes exact formula and numerical example
- Round-trip relationship documented with error bounds
- All formulas use ONNX spec variable names (y_scale, y_zero_point, x_scale, x_zero_point)
- No hardware pseudocode (per user decision)
- Document uses markdown math syntax that renders on GitHub
  </done>
</task>

</tasks>

<verification>
All Phase 10 success criteria verified:

1. QuantizeLinear documentation includes exact formula `q = saturate(round(x/scale) + zero_point)`, numerical example - NO hardware pseudocode (per user decision)
2. DequantizeLinear documentation includes exact formula `x = (q - zero_point) * scale`, numerical example - NO hardware pseudocode (per user decision)
3. Documentation renders correctly on GitHub with LaTeX math equations (verified by markdown math syntax)
4. Boundary operations documentation explains FP32-to-INT8 and INT8-to-FP32 conversions at model input/output
</verification>

<success_criteria>
- docs/quantization/01-boundary-operations.md exists
- Document covers QuantizeLinear with formula, parameters, symmetric/asymmetric cases, numerical example
- Document covers DequantizeLinear with formula, parameters, numerical example
- Round-trip error analysis included with mathematical proof
- All variable names match ONNX specification exactly
- Formulas use GitHub-compatible markdown math syntax
- Requirements BOUND-01 and BOUND-02 satisfied (minus hardware pseudocode which was explicitly deferred)
</success_criteria>

<output>
After completion, create `.planning/phases/10-boundary-operations-documentation/10-01-SUMMARY.md`
</output>
