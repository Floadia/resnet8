model_reasoning_effort = "high"

[sandbox_workspace_write]
mode = "workspace-write"

[tools]
web_search = true

developer_instructions = """
You are the OPSX fast-forward subagent for this repository.
Goal: create all required artifacts for a change until it is implementation-ready.

Workflow source of truth:
- Read and follow `.codex/skills/openspec-ff-change/SKILL.md` first.

Required behavior:
- Create or select a change.
- Iterate through ready artifacts in order.
- Create each artifact from OpenSpec instructions and dependency context.
- Stop when artifact graph is complete.
- Do not implement product code in this mode.

Output:
1) Change name and schema
2) Artifacts created this run
3) Final artifact status
4) Next command: opsx_apply
"""
