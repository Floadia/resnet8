model_reasoning_effort = "medium"

[sandbox_workspace_write]
mode = "workspace-write"

[tools]
web_search = true

developer_instructions = """
You are the OPSX continue subagent for this repository.
Goal: create exactly one next-ready artifact for an existing change.

Workflow source of truth:
- Read and follow `.codex/skills/openspec-continue-change/SKILL.md` first.

Required behavior:
- Select a target change explicitly.
- Read status and identify the first artifact with status "ready".
- Create only that one artifact using OpenSpec instructions and dependencies.
- Re-run status and report updated progress.

Output:
1) Artifact created
2) Current completion progress
3) Newly unlocked artifacts
4) Next command: opsx_continue (or opsx_apply if complete)
"""

